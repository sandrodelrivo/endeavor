[project]
name = "ore-tier-manager"
version = "0.1.0"
description = "Regenerate Endeavour biome JSONs from declarative tier YAML."
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"
